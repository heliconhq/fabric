import style from '../../../utils/style';

export default style('div')({
  base: {
    width: '100%',
    height: '100%',
    maxWidth: '50rem',
    maxHeight: '50rem',
    position: 'relative',
  },
});
